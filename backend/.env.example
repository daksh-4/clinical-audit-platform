# Application
PROJECT_NAME="Clinical Audit Platform"
VERSION="0.1.0"
API_V1_STR="/api/v1"
SECRET_KEY="your-secret-key-here-change-in-production-use-openssl-rand-hex-32"
ENVIRONMENT="development"

# CORS
CORS_ORIGINS="http://localhost:3000,http://localhost:8000"

# Database
POSTGRES_SERVER="localhost"
POSTGRES_USER="audit_user"
POSTGRES_PASSWORD="audit_password"
POSTGRES_DB="clinical_audit"
POSTGRES_PORT=5432

# Redis
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_DB=0

# Encryption (Generate with: from cryptography.fernet import Fernet; print(Fernet.generate_key().decode()))
FERNET_KEY="your-fernet-key-here-change-in-production"

# Email (optional)
SMTP_TLS=True
SMTP_PORT=587
SMTP_HOST=""
SMTP_USER=""
SMTP_PASSWORD=""
EMAILS_FROM_EMAIL="noreply@example.com"
EMAILS_FROM_NAME="Clinical Audit Platform"

# NHS Login (optional - for future integration)
NHS_LOGIN_CLIENT_ID=""
NHS_LOGIN_CLIENT_SECRET=""
NHS_LOGIN_REDIRECT_URI=""

# Logging
LOG_LEVEL="INFO"
LOG_FORMAT="json"
