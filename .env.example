# Root env file used by docker-compose.yml (DO NOT COMMIT your real .env)
#
# Setup:
#   cp .env.example .env
#   # then generate fresh keys:
#   python -c "import secrets; print(secrets.token_hex(32))"
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"

# Database (Postgres container)
POSTGRES_USER=audit_user
POSTGRES_PASSWORD=change-me
POSTGRES_DB=clinical_audit

# Backend
DATABASE_URL=postgresql+asyncpg://audit_user:change-me@db:5432/clinical_audit
REDIS_URL=redis://redis:6379/0

# Security (rotate if ever exposed)
SECRET_KEY=change-me
FERNET_KEY=change-me

# App
ENVIRONMENT=development
CORS_ORIGINS=["http://localhost:3000","http://localhost:8000","http://frontend:3000"]
